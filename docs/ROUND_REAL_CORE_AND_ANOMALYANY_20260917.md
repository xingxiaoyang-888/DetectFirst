# 真实缺陷保护与专用生成器：有限开发轮

用户于 2026-09-17 授权两线推进：少量允许训练的真实缺陷用于验证保护核心、改变外围；小规模评估专用缺陷生成器。先分别复核，再决定组合。本文为本轮补充，不改写既有失败记录或正式训练协议。

## A：真实核心保护

- 固定选择已有 support_ids.json 中 repeat 11、carpet/hazelnut 各前两张，均属于当次 K=5 支持范围；不得查看未选支持、校准或 real_test 的内容来替换。
- 四个 ID：mvtec/carpet/466ee643aadfca789cab、mvtec/carpet/5965ac2cc93cb74b6b52、mvtec/hazelnut/9ff8cc7816e85fee2912、mvtec/hazelnut/b2c6ac0e9359a16ddec9。对应已知标签：carpet hole/cut、hazelnut hole/hole；选择源于固定支持次序，不代表覆盖全部缺陷。
- 文件原路径中的 test 是 MVTec 官方历史划分；合法性依据冻结的新角色清单。记录新旧角色、支持清单哈希及当次 selected IDs。
- 官方 M 最近邻变换到 512×512，构造包含必要上下文的候选保护区 G、正常过渡带 Q，以及 G 外的局部编辑范围 E。保存原图/掩码/叠图并检查 E 非空、E 与 G 不交、M 包含于 G，记录构造参数。候选上下文保护不等于人工批准。
- 现有固定 FLUX Fill 50 步/guidance30/BF16：每父图两个固定 seed 的正常材质重建，共 8 次调用。输入真实缺陷图，只允许外围正常材质变化，不能抹除、重画或新增缺陷。
- 保存 raw 输出以及保护区回填后的 final 输出。final 在 G 内逐像素等于原图，外部仍需实际变化；记录 raw 在 G 内的漂移、final 最大误差、外围改变比例/强度、边界放大和可见异常。
- 像素回填实现零误差属于构造性质，不是神经网络学会不变性的证据。本轮仅为缺陷状态固定的多条件对照，不伪称已有可信正常反事实或完整六视图。
- 真实支持图用于生成条件属于新增使用方式：本轮记录 unique real conditioning IDs/count，不冒称 K_ref=0；专用生成器 B 保持不用异常参考。正式协议/公平基线后续另行冻结。

## B：AnomalyAny 小样本评估

- 优先官方 EPFL-IMOS/AnomalyAny，冻结仓库 commit、权重 revision、依赖和全部适配补丁；独立环境，不覆盖现有训练/FLUX 环境。使用作者完整正常图引导、异常注意力优化及提示细化路径，不能将普通 Stable Diffusion 冒充该方法。
- 选正常训练父图：carpet 使用首轮 074.png，加同类另一张固定次序父图；hazelnut 固定次序两张。每张两个预定 seed，共最多 8 张。缺陷概念和提示在出图前固定，采用作者产品示例/一般概念，不查看保留测试图定提示。
- 允许下载官方基础权重；CPU/登录节点下载和校验后再申请 GPU。禁止直接使用可能学过我们的测试图的第三方缺陷微调权重/生成数据。权重暂不可用时报告确切阻塞，A 继续。
- 采用作者实际推理设置并完整记录，不强制套用 FLUX 的 50/30；注意 token 对应位置、文本截断、mask 语义、seed、精度及内存优化是否改算法。所有适配需保存补丁。
- 保存原生结果及原图对照、每图耗时/显存/失败。评估真实感、缺陷可见性、正常材质/几何保存、可标注性及同源条件构造适配性；不能因像素发生变化就宣称缺陷真实。

## 资源和交付

- 最多同时 2×实际 L40；一线一卡，依赖准备齐备后申请。A 首个 job 上限 30 分钟；B 首个 job 上限 60 分钟；所有重试计入原累计 4 L40 卡时预算，已有 673 GPU 秒。本轮最多新增 16 张，记入开发 120 次预算，旧 6 次保留。
- 不运行本地项目测试；必要验证在服务器执行。无分割训练、无正式扩量、无改动原严格数值验收。
- 两条线各保存独立 outputs/<round>/<lane>/，交付 README、manifest、逐图 PNG、对照图、原始日志、程序/模型版本、机器检查、AI 初检和明确的人审待办；种子跨 JSON 工具传递使用精确十进制字符串。
- 两名人工审核未完成前 reviewer/status 保持待审。保留全部失败，按固定样本分母报告，不重抽到好看为止。
- A 构造检查和 B 质量评估分别通过后，再设计兼容的组合轮；本轮不自动拼接或声称研究有效。
