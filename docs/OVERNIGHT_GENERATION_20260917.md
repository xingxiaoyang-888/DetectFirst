# 2026-09-17 夜间自主生成窗口

用户最新授权：约睡眠 7 小时，继续推进并生成样本；质量达标且完整数据管线可运行时按需增加显卡，避免非必要 token 与计算浪费。本授权覆盖此前“只完成 A8/B8 后停止”的限制，但不将失败或未人审结果改为合格。

窗口截止：2026-09-17 09:33:00 Asia/Shanghai，即 2026-09-17T01:33:00Z。用户提前回来要求停止则更早收尾。完成预定数据、达到资源限额或确实无可推进项时可提前结束。

## 先解决当前阻塞

读取本轮 round_status 与最新 Slurm 结果；现有 probe2721694 排队，核对后复用。比较已成功 FLUX 环境与隔离 AnomalyAny 环境的 CUDA/设备绑定，不跳过设备检查、不修改共享驱动、不动无关 ACL 作业。若指定节点久排而其他合法 L40 可用，可取消尚未运行的本项目探针并提交一个独立短对照探针，禁止堆积重复探针。

AnomalyAny 全部权重与环境已准备，完整方法/输入适配记录必须保留；先完成此前固定四父图两 seed 的最多8张，按实测调整微批或资源，但不能关闭核心注意力/提示梯度来冒充方法成功。网络、依赖和代码的必要修复可自主执行，失败记账。

## 两线质量检查与有限改进

A 的8张机械不变量通过但语义未通过，原失败不覆盖。新修订可针对已观察的新增外围异常，固定一套更局部的材质编辑范围、必要物体区域限制和上下文保护策略，在同一已允许支持集中作有记录的小批对照；不得靠重抽同一配置直到好看。每条路线每个系统性问题最多两轮有具体假设的 recipe 修订；质量仍差则暂停该路线，推进另一条独立工作。

每批保存全量原始/最终图、source、M/G/Q/E或候选mask、差分、边界放大、prompt/精确seed、模型/代码版本、调用时间显存、失败原因与数据来源。AI必须实际看每批对照图，尺寸或差分通过不能代替语义判断。核心保持、外围正常性、缺陷可见及真实感、边界/标签可用性分别报告。生成缺陷的注意力mask不能直接冒充像素真值。

若 B 缺陷可信而 A 外围仍不可靠，不自动组合；反之也一样。两线候选通过后才实现组合：固定同一缺陷核心并构造正常/异常两状态，分别搭配多个合法条件，核对实际分割预处理后的不变量。真实缺陷线的正常反事实如需修复生成，也必须单列审核，不能把擦除失败当正常。复用真实异常的 unique IDs/用途、合成核心来源均记录；不声称 K_ref=0。原严格分割器数值审核 FAIL 保持，夜间不启动分割训练。

## 放大条件与数量

优先完成最多120次累计开发生成预算（当前14次，B尚0）；以8至12张小批推进，并保留所有尝试分母。原A8、旧失败6次及后续B/修订均记账，不无限补齐合格数。

只有完整的生成→标注候选→组合→机器检查→逐图AI初检→谱系与收据管线跑通，且冻结配置的连续两批各至少10/12个样本通过预先记录的AI准入项、没有系统性新增外围缺陷或标签错位，才允许进一步生成正式候选。该阈值仅为工程放量依据，不是人工批准或研究有效性证明；记录实际通过/拒绝原因，不因结果修改阈值。

符合条件后可自动准备一个尚未参与调参的正式产品的合法数据划分，按原协议最多50正常父图、200核心尝试、每核心两个正常编辑条件运行，最多600次生成调用；不读取其保留测试图/标签来调recipe。若正式资产/角色/生成器适配尚未齐全则继续已授权开发整理，不带病放量。全窗口最多上述120开发预算加首正式产品600调用，不改总体24单元/200核心规划，不扩其他正式产品。

所有产物保持人工待审，AI通过单独记录，禁止填写虚假reviewer或建立声称人审合格的正式训练池。用户此次授权允许先自动生成/检查候选；无需因用户睡眠暂停已授权候选工作。

## 资源、截止与节省

- 起步单卡，必要时两条独立就绪流水各一卡；仅质量及吞吐确有收益后扩到最多4张实际L40，并行独立任务，不同线程抢一个offload pipeline。暂不改用A800。
- 本窗口新增L40使用上限8卡时（28,800 GPU秒），已发生项目943秒单独记为窗口基线；此保守夜间限额是代理执行上限，替代旧累计4卡时限制。不是要求用满。失败探针和重试均消耗预算。
- 下载/排队/等待审核不占GPU；各GPU工作批完成即退出释放。未就绪任务不提前占卡。提交前原子核算累计实耗及所有未完成作业最坏预留，防并行超支。
- 每个新作业都有逐任务时限。执行脚本开头按当前UTC计算离窗口截止的剩余秒，用超时守卫覆盖整个GPU负载；即使排队到窗口末才启动，也应直接退出而非再跑满时限。每张图之间再次检查截止/调用数/预算。不要仅依赖本地定时任务释放服务器资源。
- 截止时只取消账本中本项目仍在等待/运行的确切job ID，核对WorkDir/名称；不使用scancel -u。保存完成图片与失败/中断收据，不触碰其他项目。
- 只用当前执行任务的现有defectfirst每10分钟heartbeat；无变化读紧凑状态后结束，不反复读完整日志、做网页检索或给主任务发无进展消息。不要另建重复主任务监控。
- 完成、到点、资源上限或外部硬阻塞后整理 MORNING_REPORT.md：每线尝试/技术成功/AI通过/人工待审、代表与失败图片路径、最终配置、剩余问题、实际GPU账单、所有作业释放情况。最后一次检查后暂停本heartbeat；不让夜间授权变成无限期续跑。

本地仅编辑/同步代码与记录，项目验证仍在服务器执行。不创建新子任务，不启动分割训练，不重写历史失败和旧验收结果。
